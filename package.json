{
  "name": "channel-grapher",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.24.0",
    "moment-range": "^4.0.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-moment": "^0.9.2",
    "react-scripts": "0.9.5"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -Rf resources/static && mv  -f build resources/static",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
